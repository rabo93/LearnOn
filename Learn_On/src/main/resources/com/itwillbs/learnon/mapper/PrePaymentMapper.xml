<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itwillbs.learnon.mapper.PrePaymentMapper">
	
	<!-- 사전 결제 정보 DB 저장 -->
	<insert id="save">
		INSERT INTO PAYTEST (
			MERCHANT_UID
			, CLASS_NAME
			, MEM_NAME
			, PRICE
			, PAY_METHOD
			, PAY_STATUS -- 1:결제완료, 2:취소, 3:입금대기(default)
			, PAY_DATE
		)
		VALUES (
			#{merchantUid}
			, #{className}
			, #{memName}
			, #{price}
			, #{payMethod}
			, 3	
			, CURRENT_TIMESTAMP
		); 
	</insert>
	
	<!-- 사후 결제 정보(포트원에서 응답받은 데이터) 사전 데이터와 비교 검증 -->
	<select id="findById">
	
	</select>
	
</mapper>